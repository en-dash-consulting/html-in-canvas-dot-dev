{
  "name": "html-in-canvas-dev",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "og": "node scripts/generate-og-image.mjs",
    "llms-full": "node scripts/generate-llms-full.mjs",
    "prebuild": "npm run og && npm run llms-full",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "astro": "astro",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.1.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "@playwright/test": "^1.59.1",
    "node-html-parser": "^7.1.0",
    "typescript": "^5.9.3"
  }
}
